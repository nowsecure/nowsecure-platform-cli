fragment AssessmentAnalysisFields on AutoAssessmentAnalysis {
  task {
    dynamic {
      ...JobFields
    }
    static {
      ...JobFields
    }
  }
  status
  isRunning
  isComplete
  isCancelled
  errorCode
}
